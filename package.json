{
  "name": "ai-productivity-saas",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client && npm install --prefix tests",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "test": "npm test --prefix server",
    "lint": "npm run typecheck --prefix server && npm run typecheck --prefix client",
    "test:e2e": "npm test --prefix tests"
  }
}
